services:
  web:
    build:
      context: .
      args:
        NEXT_PUBLIC_API_URL: http://localhost:3001/api/v1
        NEXT_PUBLIC_SHOW_DEPOIMENTOS: "true"
    image: federal-concursos-frontend
    container_name: federal-concursos-frontend
    ports:
      - "3000:3000"
    restart: always
